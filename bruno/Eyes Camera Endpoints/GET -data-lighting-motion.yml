info:
  name: GET /data/lighting/motion
  type: http
  seq: 24

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/motion
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
