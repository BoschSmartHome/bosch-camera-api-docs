info:
  name: GET /data/privacyState
  type: http
  seq: 2

http:
  method: GET
  url: https://{{camera_host}}/sh/data/privacyState
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
