info:
  name: GET /data/wifiInfo
  type: http
  seq: 30

http:
  method: GET
  url: https://{{camera_host}}/sh/data/wifiInfo
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
