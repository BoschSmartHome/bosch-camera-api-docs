info:
  name: GET /data/sh-controller/paired
  type: http
  seq: 1

http:
  method: GET
  url: https://{{camera_host}}/sh/data/sh-controller/paired
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
