info:
  name: GET /data/audio/currentNoise
  type: http
  seq: 19

http:
  method: GET
  url: https://{{camera_host}}/sh/data/audio/currentNoise
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
