info:
  name: GET /data/statusLed
  type: http
  seq: 20

http:
  method: GET
  url: https://{{camera_host}}/sh/data/statusLed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
