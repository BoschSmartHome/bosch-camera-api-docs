info:
  name: GET /data/intrusionSystem/arming
  type: http
  seq: 10

http:
  method: GET
  url: https://{{camera_host}}/sh/data/intrusionSystem/arming
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
