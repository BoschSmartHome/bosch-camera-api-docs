info:
  name: GET /data/alarm/status
  type: http
  seq: 13

http:
  method: GET
  url: https://{{camera_host}}/sh/data/alarm/status
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
