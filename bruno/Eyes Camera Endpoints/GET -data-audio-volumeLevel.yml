info:
  name: GET /data/audio/volumeLevel
  type: http
  seq: 18

http:
  method: GET
  url: https://{{camera_host}}/sh/data/audio/volumeLevel
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
