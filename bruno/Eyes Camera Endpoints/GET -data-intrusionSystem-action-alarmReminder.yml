info:
  name: GET /data/intrusionSystem/action/alarmReminder
  type: http
  seq: 11

http:
  method: GET
  url: https://{{camera_host}}/sh/data/intrusionSystem/action/alarmReminder
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
