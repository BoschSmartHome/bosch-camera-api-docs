info:
  name: GET /data/lighting/config
  type: http
  seq: 26

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
