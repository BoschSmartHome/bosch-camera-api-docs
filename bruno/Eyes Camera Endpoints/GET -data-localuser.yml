info:
  name: GET /data/localUser
  type: http
  seq: 31

http:
  method: GET
  url: https://{{camera_host}}/sh/data/localUser
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
