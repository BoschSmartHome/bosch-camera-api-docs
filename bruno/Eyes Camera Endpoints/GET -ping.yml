info:
  name: GET /ping
  type: http
  seq: 5

http:
  method: GET
  url: https://{{camera_host}}/sh/ping
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
