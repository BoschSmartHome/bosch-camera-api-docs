info:
  name: GET /data/lighting/manual
  type: http
  seq: 23

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/manual
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
