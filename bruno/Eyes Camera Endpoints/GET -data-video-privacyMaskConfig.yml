info:
  name: GET /data/video/privacyMaskConfig
  type: http
  seq: 29

http:
  method: GET
  url: https://{{camera_host}}/sh/data/video/privacyMaskConfig
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
