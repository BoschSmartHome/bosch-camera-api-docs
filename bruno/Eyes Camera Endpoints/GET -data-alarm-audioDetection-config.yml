info:
  name: GET /data/alarm/audioDetection/config
  type: http
  seq: 17

http:
  method: GET
  url: https://{{camera_host}}/sh/data/alarm/audioDetection/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
