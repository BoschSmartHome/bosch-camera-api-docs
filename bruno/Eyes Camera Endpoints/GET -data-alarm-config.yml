info:
  name: GET /data/alarm/config
  type: http
  seq: 12

http:
  method: GET
  url: https://{{camera_host}}/sh/data/alarm/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
