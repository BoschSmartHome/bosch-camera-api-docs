info:
  name: GET /data/calibration/extrinsic
  type: http
  seq: 16

http:
  method: GET
  url: https://{{camera_host}}/sh/data/calibration/extrinsic
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
