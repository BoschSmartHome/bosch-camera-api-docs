info:
  name: GET /data/intrusionSystem/config
  type: http
  seq: 15

http:
  method: GET
  url: https://{{camera_host}}/sh/data/intrusionSystem/config
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
