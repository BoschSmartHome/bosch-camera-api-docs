info:
  name: GET /data/lighting/level
  type: http
  seq: 22

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/level
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
