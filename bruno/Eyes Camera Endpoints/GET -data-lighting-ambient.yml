info:
  name: GET /data/lighting/ambient
  type: http
  seq: 25

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/ambient
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
