info:
  name: GET /data/privacyMode/jingle
  type: http
  seq: 4

http:
  method: GET
  url: https://{{camera_host}}/sh/data/privacyMode/jingle
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
