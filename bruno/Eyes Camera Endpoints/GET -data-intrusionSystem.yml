info:
  name: GET /data/intrusionSystem
  type: http
  seq: 9

http:
  method: GET
  url: https://{{camera_host}}/sh/data/intrusionSystem
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
