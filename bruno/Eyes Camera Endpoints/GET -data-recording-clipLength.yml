info:
  name: GET /data/recording/clipLength
  type: http
  seq: 28

http:
  method: GET
  url: https://{{camera_host}}/sh/data/recording/clipLength
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
