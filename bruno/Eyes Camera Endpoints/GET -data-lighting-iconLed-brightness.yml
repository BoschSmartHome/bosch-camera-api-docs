info:
  name: GET /data/lighting/iconLed/brightness
  type: http
  seq: 21

http:
  method: GET
  url: https://{{camera_host}}/sh/data/lighting/iconLed/brightness
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
