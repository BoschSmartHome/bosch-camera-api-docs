info:
  name: GET /data/privacyMode
  type: http
  seq: 3

http:
  method: GET
  url: https://{{camera_host}}/sh/data/privacyMode
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
