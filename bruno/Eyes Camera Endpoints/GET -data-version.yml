info:
  name: GET /data/version
  type: http
  seq: 6

http:
  method: GET
  url: https://{{camera_host}}/sh/data/version
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
